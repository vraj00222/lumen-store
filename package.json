{
  "name": "lumen-store",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Lumen — a demo storefront monitored by Capsule. Crashes are auto-captured; the AI agent opens fix-PRs against this repo.",
  "scripts": {
    "start": "tsx src/server.ts",
    "dev": "tsx src/server.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
